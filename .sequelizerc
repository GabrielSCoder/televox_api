const path = require('path');

module.exports = {
  config: path.resolve('src', 'config', 'config.js'),  // Configuração dentro de src/config
  'models-path': path.resolve('src', 'models'),        // Models dentro de src/models
  'seeders-path': path.resolve('src', 'seeders'),      // Seeders dentro de src/seeders
  'migrations-path': path.resolve('src', 'migrations') // Migrations dentro de src/migrations
};
